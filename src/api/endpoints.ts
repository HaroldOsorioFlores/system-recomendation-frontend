import { urlApi } from "./url";

export const urlRegisterUser = `${urlApi}/register`;
export const urlLoginUser = `${urlApi}/login`;

export const urlRecomendations = `${urlApi}/recommendations`;

export const urlHistoryRecomendations = `${urlApi}/history-recomendations`;
